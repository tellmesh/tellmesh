[project]
name = "desktop-operator-agent"
version = "0.1.0"
description = "Isolated desktop operator agent for TellMesh"
requires-python = ">=3.10"
dependencies = [
  "uri2ops",
  "fastapi>=0.115",
  "uvicorn>=0.27",
]

[tool.uv.sources]
uri2ops = { workspace = true }
