# Example 06 — kontrakt agenta zamówień

Przykładowy kontrakt agenta do obsługi zamówień (`read_order`, `read_order_events`, `create_order`).

Plik:

```txt
examples/06_orders_agent/create_orders_agent.yaml
```

Można go użyć jako wzorca przy ręcznym tworzeniu `contracts/agents/*.yaml` albo jako punkt odniesienia dla proposal evolution w [`../08_evolution/proposals/add_orders_agent.yaml`](../08_evolution/proposals/add_orders_agent.yaml).
