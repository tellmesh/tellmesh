# Registry indexes

Generated by:

```bash
uri3 doctor --build-registry
```

Files:

- `capability_index.json` — touri capability manifests
- `operation_index.json` — uri2ops operation registry
- `uri_index.json` — sample URIs and resolution (`uri3 explain`)

Regenerate after adding capabilities or operator operations.
